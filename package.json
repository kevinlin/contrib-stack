{
  "name": "contrib-stack",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "test": "vitest run",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "vitest": "^3.2.1",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@10.31.0",
  "engines": {
    "node": ">=22"
  }
}
